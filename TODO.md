## TODO

- [x] Update `scraper/utils/helpers.py`: add `requirements` parameter to `build_job_dict()` and include `requirements` in return dict
- [x] Update all 12 scraper sources to pass `requirements_text` into `build_job_dict()`
- [ ] Commit + push changes

